enum Errors {
    invalid_username, user_not_found, invalid_user_data, invalid_password, token_creation_fail,
    bad_email, bad_password, bad_name, meow_not_found, bad_follow,
    database_error, invalid_message
}

export default Errors;